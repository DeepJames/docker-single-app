sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t camperjames/docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=true camperjames/docker-react-app num run test --coverage

after_success:
  - echo "Test Success"